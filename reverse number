#include <iostream>
using namespace std;
int main() 
{
    int number,rem,num;
    cout<<"Enter no: ";
    cin>>number;
    while(number!=0)
    {
        rem=number%10;
        num=num*10+rem;
        number/=10;
    }
   cout<<"reverse number: "<<num;

    return 0;
}
