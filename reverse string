#include <iostream>
#include <string>
using namespace std;
int main() 
{
    
   string s;
   cout<<"Enter string: ";
   cin>>s;
   string reverse;
   for(int i=s.length()-1;i>=0;i--)
   {
       reverse=reverse+s[i];
   }
   cout<<reverse<<endl;

    return 0;
}
